module DOM.HTML.HTMLLegendElement where

import Prelude (Unit())

import Control.Monad.Eff (Eff())

import DOM (DOM())
import DOM.HTML.Types (HTMLLegendElement())

-- interface HTMLLegendElement : HTMLElement {
--   readonly attribute HTMLFormElement? form;
-- };
